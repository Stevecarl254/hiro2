import '../styles/global.css'

export const metadata = {
  title: 'Hiro Catering',
  description: 'One-Stop Solution For All Catering Services & Event Needs',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className="antialiased">
        {children}
      </body>
    </html>
  )
}
