// components/HeroSection.jsx
'use client';
import { useEffect, useState } from 'react';

const HeroSection = () => {
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    setIsVisible(true);
  }, []);

  return (
    <section className="min-h-screen bg-gray-900 relative overflow-hidden">
      {/* Solid dark background */}
      <div className="absolute inset-0 bg-gray-900"></div>

      <div className="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 min-h-screen flex items-center justify-center">
        <div className="w-full text-center">
          <div className={`transition-all duration-1000 ${isVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'}`}>
            
            {/* Main Heading */}
            <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-8 leading-tight">
              One-Stop Solution
              <br />
              <span className="text-gray-300">For All Catering Services</span>
              <br />
              <span className="text-white">& Event Needs</span>
            </h1>

            {/* Single CTA Button - More prominent */}
            <div className="mt-16">
              <button className="bg-gradient-to-r from-blue-600 to-blue-700 text-white px-10 py-5 rounded-xl font-bold text-xl hover:shadow-2xl transition-all duration-300 transform hover:scale-105 shadow-lg border-2 border-blue-500/30">
                Click before to get a quote and let's score your event right.
              </button>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
};

export default HeroSection;
