export default function WhyChooseUs() {
  const reasons = [
    {
      title: "Verified Professionals",
      desc: "All service providers are screened for quality and reliability.",
    },
    {
      title: "Affordable Packages",
      desc: "Transparent pricing and packages that fit your budget.",
    },
    {
      title: "Quick Booking",
      desc: "Book everything you need in just a few clicks.",
    },
  ];

  return (
    <section id="why" className="py-20 bg-gray-50">
      <div className="max-w-6xl mx-auto px-6 text-center">
        <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-6">Why Choose Us</h2>
        <p className="text-gray-600 mb-10 max-w-2xl mx-auto">
          We connect event planners, caterers, and equipment providers — making event prep fast and effortless.
        </p>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {reasons.map((r) => (
            <div key={r.title} className="bg-white p-6 rounded-2xl shadow-sm hover:shadow-md transition">
              <h3 className="text-xl font-semibold text-emerald-600 mb-2">{r.title}</h3>
              <p className="text-gray-500">{r.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
