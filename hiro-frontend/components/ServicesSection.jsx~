export default function ServicesSection() {
  return (
    <section id="services" className="py-20 bg-white">
      <div className="max-w-6xl mx-auto px-6 text-center">
        <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
          Our Services
        </h2>
        <p className="text-gray-600 mb-10 max-w-2xl mx-auto">
          Hire professional caterers and rent catering tools — all in one place.
        </p>

        <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
          {[
            { title: "Caterers", desc: "Discover experienced caterers ready to make your event special." },
            { title: "Tool Rentals", desc: "From tents to serving trays — everything you need for your event." },
            { title: "Event Packages", desc: "Bundle catering services and tools for convenience and savings." },
          ].map((service) => (
            <div key={service.title} className="p-6 bg-gray-50 rounded-2xl shadow-sm hover:shadow-md transition">
              <h3 className="text-xl font-semibold text-emerald-600 mb-2">{service.title}</h3>
              <p className="text-gray-500">{service.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
